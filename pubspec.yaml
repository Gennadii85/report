name: pdf_invoice_generator_flutter
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  bloc: ^8.1.2
  cupertino_icons: ^1.0.3
  dropdown_button2: ^2.3.9
  file_picker: ^6.1.1
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.3
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  image_picker: ^1.0.7
  open_file: ^3.3.2
  path_provider: ^2.0.5
  pdf: ^3.8.2

dev_dependencies:
  flutter_lints: ^3.0.1
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/logo.jpg
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: assets/open-sans.ttf
